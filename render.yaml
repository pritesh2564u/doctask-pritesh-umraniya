services:
  # PostgreSQL Database
  - type: pserv
    name: doctask-postgres
    plan: starter
    ipAddress: private
    databaseName: doctask
    user: postgres
    
  # Backend API Service
  - type: web
    name: doctask-backend
    runtime: docker
    plan: starter
    rootDir: backend
    dockerfilePath: Dockerfile
    startCommand: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - port: 8000
        protocol: HTTP
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: doctask-postgres
          property: connectionString
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_MODEL
        value: openai/gpt-oss-20b
    depends:
      - type: pserv
        name: doctask-postgres
        
  # Frontend Service
  - type: web
    name: doctask-frontend
    runtime: docker
    plan: starter
    rootDir: frontend
    dockerfilePath: Dockerfile
    ports:
      - port: 80
        protocol: HTTP
    envVars:
      - key: VITE_API_URL
        value: https://doctask-backend.onrender.com
      - key: NODE_ENV
        value: production
    depends:
      - type: web
        name: doctask-backend
